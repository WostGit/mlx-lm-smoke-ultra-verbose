name: MLX Swift Minimum Working Smoke

on:
  workflow_dispatch:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: mlx-swift-minimum-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  core-smoke:
    name: MLX Swift minimum working smoke on macOS
    runs-on: macos-15
    timeout-minutes: 60

    env:
      DEVELOPER_DIR: /Applications/Xcode.app/Contents/Developer
      NSUnbufferedIO: "YES"
      CLICOLOR: "1"

    steps:
      - name: Check out wrapper repository
        uses: actions/checkout@v5

      - name: Show runner and toolchain info
        shell: bash
        run: |
          set -euxo pipefail
          uname -a
          sw_vers
          sysctl -n machdep.cpu.brand_string || true
          xcode-select -p
          xcodebuild -version
          swift --version
          pwd
          ls -la

      - name: Clone mlx-swift with submodules
        shell: bash
        run: |
          set -euxo pipefail
          git clone --recursive https://github.com/ml-explore/mlx-swift.git
          cd mlx-swift
          git rev-parse HEAD
          git submodule status
          test -f Source/Cmlx/mlx/mlx/backend/cpu/compiled.cpp

      - name: Run upstream package tests with xcodebuild
        shell: bash
        run: |
          set -euxo pipefail
          cd mlx-swift
          xcodebuild \
            test \
            -scheme mlx-swift-Package \
            -destination 'platform=macOS' \
            -derivedDataPath "$PWD/DerivedData" \
            -resultBundlePath "$PWD/MLXSwiftPackageTests.xcresult" \
            -showBuildTimingSummary

      - name: Upload core artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mlx-swift-minimum-working-artifacts
          if-no-files-found: ignore
          path: |
            mlx-swift/MLXSwiftPackageTests.xcresult

  experimental-qwen:
    name: Experimental Qwen text smoke
    runs-on: macos-15
    timeout-minutes: 45
    needs: core-smoke
    continue-on-error: true

    env:
      DEVELOPER_DIR: /Applications/Xcode.app/Contents/Developer
      NSUnbufferedIO: "YES"
      CLICOLOR: "1"

    steps:
      - name: Check out wrapper repository
        uses: actions/checkout@v5

      - name: Show runner and toolchain info
        shell: bash
        run: |
          set -euxo pipefail
          uname -a
          sw_vers
          xcode-select -p
          xcodebuild -version
          swift --version
          pwd
          ls -la

      - name: Create experimental Qwen smoke package
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p smoke-qwen
          cd smoke-qwen

          cat > Package.swift <<'SWIFT'
          // swift-tools-version: 6.1
          import PackageDescription

          let package = Package(
              name: "SmokeQwen",
              platforms: [.macOS(.v14)],
              dependencies: [
                  .package(url: "https://github.com/ml-explore/mlx-swift-lm", .upToNextMajor(from: "3.31.3")),
                  .package(url: "https://github.com/huggingface/swift-huggingface", from: "0.9.0"),
                  .package(url: "https://github.com/huggingface/swift-transformers", from: "1.3.0")
              ],
              targets: [
                  .executableTarget(
                      name: "SmokeQwen",
                      dependencies: [
                          .product(name: "MLXLLM", package: "mlx-swift-lm"),
                          .product(name: "MLXLMCommon", package: "mlx-swift-lm"),
                          .product(name: "MLXHuggingFace", package: "mlx-swift-lm"),
                          .product(name: "HuggingFace", package: "swift-huggingface"),
                          .product(name: "Tokenizers", package: "swift-transformers")
                      ]
                  )
              ]
          )
          SWIFT

          mkdir -p Sources/SmokeQwen
          cat > Sources/SmokeQwen/main.swift <<'SWIFT'
          import Foundation
          import HuggingFace
          import Tokenizers
          import MLXLLM
          import MLXLMCommon
          import MLXHuggingFace

          @main
          struct SmokeQwen {
              static func main() async throws {
                  print("Starting experimental Qwen text-only smoke test.")
                  print("About to load mlx-community/Qwen2.5-0.5B-Instruct-4bit")

                  let model = try await loadModel(
                      from: #hubDownloader(),
                      using: #huggingFaceTokenizerLoader(),
                      id: "mlx-community/Qwen2.5-0.5B-Instruct-4bit"
                  )

                  let session = ChatSession(model)
                  let response = try await session.respond(to: "Reply with exactly OK")

                  print("=== RESPONSE BEGIN ===")
                  print(response)
                  print("=== RESPONSE END ===")
                  print("Experimental Qwen smoke test passed.")
              }
          }
          SWIFT

      - name: Resolve package
        shell: bash
        run: |
          set -euxo pipefail
          cd smoke-qwen
          swift package resolve

      - name: Show Xcode package schemes
        shell: bash
        run: |
          set -euxo pipefail
          cd smoke-qwen
          xcodebuild -list -packagePath .

      - name: Build package executable with xcodebuild
        shell: bash
        run: |
          set -euxo pipefail
          cd smoke-qwen
          xcodebuild \
            -packagePath . \
            -scheme SmokeQwen \
            -configuration Debug \
            -destination 'platform=macOS' \
            -derivedDataPath "$PWD/DerivedData" \
            build

      - name: Inspect built products
        shell: bash
        run: |
          set -euxo pipefail
          cd smoke-qwen
          find "$PWD/DerivedData/Build/Products" -maxdepth 4 | sort | sed -n '1,240p'

      - name: Run built executable from Xcode products directory
        shell: bash
        run: |
          set -euxo pipefail
          cd smoke-qwen
          PRODUCT_DIR="$(find "$PWD/DerivedData/Build/Products" -type d -name Debug | head -n 1)"
          test -n "$PRODUCT_DIR"
          cd "$PRODUCT_DIR"
          pwd
          ls -lah
          find . -maxdepth 2 | sort | sed -n '1,240p'
          ./SmokeQwen

      - name: Upload experimental artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mlx-swift-qwen-experimental-artifacts
          if-no-files-found: ignore
          path: |
            smoke-qwen/DerivedData
